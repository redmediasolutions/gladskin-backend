name: gladskin_backend
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  cloud_firestore: ^6.3.0
  firebase_auth: ^6.4.0
  csv: ^8.0.0
  firebase_core: ^4.7.0
  flutter:
    sdk: flutter
  go_router: ^17.2.2
  intl: ^0.20.2
  path_provider: ^2.1.5
  share_plus: ^13.1.0
  cloud_functions: ^6.3.1
  pdf: ^3.13.0
  printing: ^5.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
